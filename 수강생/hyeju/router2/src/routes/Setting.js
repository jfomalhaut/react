import React from 'react';
import './Setting.css';

const Setting = ( { match } ) => {
    return (
        <div className="container">
            <h1>Setting Complete!</h1>
        </div>
    )
};

export default Setting;
