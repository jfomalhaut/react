import React from 'react'


const About = () =>{
    return(
        <div className="aData">
            <input value={aData} onChange={onAData} sendInput={onAData}/>
        </div>
    );
};

export default About;